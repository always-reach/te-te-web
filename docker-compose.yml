version: '3'

services:
  twitter:
    build:
      context: .
      dockerfile: Dockerfile
    command: sh -c "npm run dev"
    environment: 
      - CHOKIDAR_USEPOLLING=true
    volumes:
      - ./:/usr/src/app:cached
      - /usr/src/app/twitter_good/node_modules
    ports:
      - "3000:3000"